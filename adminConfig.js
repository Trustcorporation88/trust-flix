module.exports = {
  // Admins autorizados a usar /painel, /disparos, /presentes, /upsell e /remarketing.
  // Coloque aqui o número do ADM no formato: "55" + DDD + número, sem +, sem espaço e sem traço.
  // Também aceita IDs internos do WhatsApp detectados pelo comando /meuid.
  // Exemplo:
  // additionalAdmins: ["5599999999999"],
  additionalAdmins: [],
};
